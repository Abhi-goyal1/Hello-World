<% layout("/layouts/boilerplate") %>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap');
  #searching {
  margin-top: 0%;
  margin-bottom: 40px;
}
#searching input {
  box-shadow: rgba(43, 175, 252, 0.65) 0px 0px 20px;
  border-top-left-radius: 30px;
  border-bottom-left-radius: 30px;
  border: none;
  padding: 15px;
  margin-top: 10px;
  width: 40%;
  /* box-shadow: rgba(0, 0, 0, 0.25) 0px 0.0625em 0.0625em,
    rgba(0, 0, 0, 0.25) 0px 0.125em 0.5em,
    rgba(255, 255, 255, 0.1) 0px 0px 0px 1px inset; */
}
#searching button {
  box-shadow: rgba(43, 175, 252, 0.65) 0px 0px 20px;
  border-top-right-radius: 30px;
  border-bottom-right-radius: 30px;
  border: none;
  background-color: #000;
  color: white;
  width: 20%;
  margin-top: 10px;
  font-size: 20px;
  /* box-shadow: rgba(0, 0, 0, 0.25) 0px 0.0625em 0.0625em,
    rgba(0, 0, 0, 0.25) 0px 0.125em 0.5em,
    rgba(255, 255, 255, 0.1) 0px 0px 0px 1px inset; */
}
#searching button:hover {
  background-color: black;
}
#searching input:focus {
  outline: none;
  border: 1px solid #00acee;
}
  .containers {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 20px;
  }
  #searching{
    margin-top:0% !important;
    margin-bottom:5% !important;
  }
  .footer-02{
    margin-top:0% !important;
  }
  .containers h1{
    color:white;
  }
  .containers h1 span{
    color:white;
  }
  .main-container p{
    background-color:black !important;
    border-radius:50px;
  }
  

  
  .logo {
      font-size: 1.5rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 2px;
      color: #3498db;
  }
  
  .hero {

    background: rgb(43,175,252);
background: radial-gradient(circle, rgba(43,175,252,1) 1%, rgba(255,255,255,1) 100%, rgba(255,255,255,1) 100%);
/* 
    background: rgb(255,255,255);
background: radial-gradient(circle, rgba(255,255,255,1) 0%, rgba(43,175,252,1) 100%, rgba(255,255,255,1) 100%); */
      background-size: cover;
      background-position: center;
      background-attachment: fixed;
      color: #ecf0f1;
      text-align: center;
      padding: 150px 0 100px;
      position: relative;
      overflow: hidden;
  }
  
  .hero::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: url('https://tutorialhub.io/images/pattern.png');
      opacity: 0.1;
      animation: backgroundScroll 20s linear infinite;
  }
  
  @keyframes backgroundScroll {
      from {background-position: 0 0;}
      to {background-position: 100% 100%;}
  }
  
  .hero h1 {
    color: #000000;
  font-family: 'Poppins', sans-serif;
  font-weight: 700;
    font-size: 5rem;
    margin-bottom: 1rem;
    text-shadow: 2px 2px 4px rgba(184, 177, 177, 0.893);
    animation: fadeInDown 1s ease-out;
  }
  
  .hero p {
    padding: 5px;
      font-size: 1.3rem;
      margin-bottom: 2rem;
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
      animation: fadeInUp 1s ease-out 0.5s both;
  }
  
  @keyframes fadeInDown {
      from {
          opacity: 0;
          transform: translateY(-20px);
      }
      to {
          opacity: 1;
          transform: translateY(0);
      }
  }
  
  @keyframes fadeInUp {
      from {
          opacity: 0;
          transform: translateY(20px);
      }
      to {
          opacity: 1;
          transform: translateY(0);
      }
  }
  

  .course-categories {
      padding: 30px 0 60px;
      background-color: #000;
  }
  
  .course-categories h2 {
      text-align: center;
      margin-bottom: 50px;
      font-size: 2.5rem;
      color: #3498db;
      position: relative;
  }
  
  .course-categories h2::after {
      content: '';
      display: block;
      width: 50px;
      height: 3px;
      background-color: #3498db;
      margin: 20px auto 0;
  }
  
  .category-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 30px;
  }
  
  .category-card {
      background-color: #222222;
      border-radius: 15px;
      overflow: hidden;
      box-shadow: 0 10px 30px rgba(255,255,255,0.05);
      transition: all 0.3s ease;
      position: relative;
      cursor: pointer;
  }
  
  .category-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 15px 40px rgba(255,255,255,0.1);
  }
  
  .category-icon {
      font-size: 3rem;
      color: #3498db;
      margin-bottom: 15px;
  }
  
  .category-content {
      padding: 25px;
      text-align: center;
  }
  
  .category-title {
      font-size: 1.3rem;
      margin-bottom: 15px;
      color: #3498db;
  }
  
  .category-description {
      color: #aaa;
      margin-bottom: 20px;
      font-size: 0.9rem;
  }
  
  .subcategory-count {
      display: inline-block;
      background-color: #3498db;
      color: #fff;
      padding: 5px 10px;
      border-radius: 20px;
      font-size: 0.8rem;
      margin-top: 10px;
  }
  
  .subcategories {
      list-style-type: none;
      padding: 0;
      max-height: 0;
      overflow: hidden;
      transition: max-height 0.5s ease;
  }
  
  
  @media (max-width: 768px) {
      .hero h1 {
          font-size: 2.5rem;
      }
      
      .hero p {
          font-size: 1rem;
      }
  }

/* Style for the active link */
button {
  position: relative;
  display: inline-block;
  cursor: pointer;
  outline: none;
  border: 0;
  vertical-align: middle;
  background: transparent;
  padding: 0;
  font-size: inherit;
  font-family: inherit;
}

button.learn-more {
  width: 13rem;
  height: auto;
}

button.learn-more .circle {
  transition: all 0.45s cubic-bezier(0.65, 0, 0.076, 1);
  position: relative;
  display: block;
  margin: 0;
  width: 3rem;
  height: 3rem;
  background: #000;
  border-radius: 1.625rem;
  top: 0.3rem;
}

button.learn-more .circle .icon {
  transition: all 0.45s cubic-bezier(0.65, 0, 0.076, 1);
  position: absolute;
  top: 0;
  bottom: 0;
  margin: auto;
  background: #fff;
}

button.learn-more .circle .icon.arrow {
  transition: all 0.45s cubic-bezier(0.65, 0, 0.076, 1);
  left: 0.625rem;
  width: 1.125rem;
  height: 0.125rem;
  background: none;
}

button.learn-more .circle .icon.arrow::before {
  position: absolute;
  content: "";
  top: -0.29rem;
  right: 0.0625rem;
  width: 0.625rem;
  height: 0.625rem;
  border-top: 0.125rem solid #fff;
  border-right: 0.125rem solid #fff;
  transform: rotate(45deg);
}

button.learn-more .button-text {
  transition: all 0.45s cubic-bezier(0.65, 0, 0.076, 1);
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  padding: 0.75rem 0;
  margin: 0 0 0 1.85rem;
  color: #000;
  font-weight: 800;
  line-height: 1.6;
  text-align: center;
  text-decoration: underline;
}

button:hover .circle {
  width: 100%;
}

button:hover .circle .icon.arrow {
  background: #fff;
  transform: translate(1rem, 0);
}

button:hover .button-text {
  color: #fff;
}

.cat-head {
  color: #2baffc !important;
}

/* Responsive styles for small devices */
@media (max-width: 600px) {
  button.learn-more {
    width: 9.5rem; /* Adjust the width */
  }

  button.learn-more .circle {
    width: 2rem; /* Adjust the circle size */
    height: 2rem; /* Adjust the circle size */
    
  }

  button.learn-more .circle .icon.arrow {
    left: 0.4rem; /* Adjust the arrow position */
    width: 0.75rem; /* Adjust the arrow size */
    height: 0.075rem; /* Adjust the arrow thickness */
  }

  button.learn-more .circle .icon.arrow::before {
    top: -0.2rem; /* Adjust the arrow before pseudo-element position */
    right: 0.04rem; /* Adjust the arrow before pseudo-element position */
    width: 0.4rem; /* Adjust the arrow before pseudo-element size */
    height: 0.4rem; /* Adjust the arrow before pseudo-element size */
    border-top: 0.075rem solid #fff; /* Adjust the arrow before pseudo-element border */
    border-right: 0.075rem solid #fff; /* Adjust the arrow before pseudo-element border */
  }

  button.learn-more .button-text {
    padding: 0.5rem 0; /* Adjust the padding */
    margin: 0 0 0 1.25rem; /* Adjust the margin */
    font-size: 0.8rem; /* Adjust the font size */
  }
}

@media screen and (max-width: 1205px) {
 .hero h1{
  font-size: 5em;
 }
}
@media screen and (max-width: 1120px) {
 .hero h1{
  font-size: 4.5em;
 }
}
@media screen and (max-width: 1020px) {
 .hero h1{
  font-size: 4em;
 }
}
@media screen and (max-width: 840px) {
 .hero h1{
  font-size: 3em;
 }
 .hero p{
  font-size: 1rem;
  max-width: 450px;
 }
}
@media screen and (max-width: 905px) {
    
 .hero h1{
  font-size: 2.5em;
 }
 .hero p{
  font-size: 1rem;
  max-width: 450px;
 }
}
@media screen and (max-width: 660px) {
    #searching{
    margin-bottom:15% !important;
 }
    #searching input {

width: 60%;

}
#searching button {

width: 30%;

}
 .hero h1{
  font-size: 2em;
 }
 .hero p{
  font-size: 0.7rem;
  max-width: 350px;
  margin-bottom: 1.5rem;
 }
}
@media screen and (max-width: 485px) {
 .hero h1{
  font-size: 1.7em;
 }
 .hero p{
  font-size: 0.6rem;
  max-width: 300px;
  margin-bottom: 1.3rem;
 }
 #searching{
    margin-bottom:15% !important;
 }
 #searching input {

  width: 60%;
  
}
#searching button {

  width: 30%;
  
}
}


@media screen and (max-width: 408px) {
 .hero h1{
  font-size: 1.5em;
 }
 .hero p{
  font-size: 0.5rem;
  max-width: 250px;
 }
}
@media screen and (max-width: 373px) {
 .hero h1{
  font-size: 1.4em;
 }
 .hero p{
  font-size: 0.5rem;
  max-width: 230px;
 }
}
@media screen and (max-width: 345px) {
 .hero h1{
  font-size: 1.3em;
 }
 .hero p{
  font-size: 0.4rem;
  max-width: 200px;
 }
}
@media screen and (max-width: 325px) {
 .hero h1{
  font-size: 1.2em;
 }
 .hero p{
  font-size: 0.4rem;
  max-width: 200px;
 }
}



</style>
</head>
<body>
  <main>
      <section class="hero">
          <div class="containers main-container">
            <h1>Enroll<span>.</span> Learn<span>.</span> Get Certified<span>.</span></h1>
              <p>Transform Your Skills with Our Best Expert-Led Courses</p>
              <a href="#categories"><button  class="learn-more">
                <span class="circle" aria-hidden="true">
                <span class="icon arrow"></span>
                </span>
                <span class="button-text">Explore Courses</span>
              </button></a>
                  </div>
          </div>
      </section>
      
      <section id="categories" class="course-categories">
          <div class="containers">
            <h1 class="heading text-center">Courses <span class="cat-head">Categories</span></h1>
              <div class="row">
                <div class="col-sm-12 col-md-12">
                  <form id="searching" class="d-flex justify-content-center">
                    <input id="searchInput" type="search" placeholder="Search e.g. HTML">
                    <button type="submit"><i class="fa-solid fa-magnifying-glass"></i></button>
                </form>
              </div>
              <div class="category-grid" id="categoryGrid">
                  <a href=""><div class="category-card">
                    <div class="category-content">
                        <div class="category-icon">💻</div>
                        <h3 class="category-title">Web Development</h3>
                        <p class="category-description">Master the art of creating stunning websites and web applications.</p>
                        <div class="subcategory-count">4 Subcategories</div>
                       
                    </div>
                </div></a>
                 <a href="">
                    <div class="category-card">
                        <div class="category-content">
                            <div class="category-icon">📊</div>
                            <h3 class="category-title">Data Science</h3>
                            <p class="category-description">Unlock insights from data and drive informed decision-making.</p>
                            <div class="subcategory-count">4 Subcategories</div>
                            
                        </div>
                    </div>
                 </a>
                 <a href="">
                    <div class="category-card">
                        <div class="category-content">
                            <div class="category-icon">📱</div>
                            <h3 class="category-title">Mobile Development</h3>
                            <p class="category-description">Build powerful mobile apps for iOS and Android platforms.</p>
                            <div class="subcategory-count">4 Subcategories</div>
                            
                        </div>
                    </div>
                 </a>
                 <a href="">
                     <div class="category-card">
                      <div class="category-content">
                          <div class="category-icon">🔒</div>
                          <h3 class="category-title">Cybersecurity</h3>
                          <p class="category-description">Protect digital assets and learn ethical hacking techniques.</p>
                          <div class="subcategory-count">4 Subcategories</div>
                      </div>
                  </div>
                 </a>
              </div>
          </div>
      </section>
  </main>

  <script>
  document.addEventListener('DOMContentLoaded', function() {
      // Smooth scrolling for anchor links
      document.querySelectorAll('a[href^="#"]').forEach(anchor => {
          anchor.addEventListener('click', function (e) {
              e.preventDefault();
              document.querySelector(this.getAttribute('href')).scrollIntoView({
                  behavior: 'smooth'
              });
          });
      });

      // Add animation to category cards on scroll
      const categoryCards = document.querySelectorAll('.category-card');
      const observerOptions = {
          threshold: 0.1,
          rootMargin: '0px 0px -50px 0px'
      };

      const observer = new IntersectionObserver((entries, observer) => {
          entries.forEach(entry => {
              if (entry.isIntersecting) {
                  entry.target.style.opacity = 1;
                  entry.target.style.transform = 'translateY(0) scale(1)';
                  observer.unobserve(entry.target);
              }
          });
      }, observerOptions);

      categoryCards.forEach((card, index) => {
          card.style.opacity = 0;
          card.style.transform = 'translateY(50px) scale(0.9)';
          card.style.transition = `opacity 0.5s ease ${index * 0.1}s, transform 0.5s ease ${index * 0.1}s`;
          observer.observe(card);

          // Add click event to toggle subcategories
          card.addEventListener('click', function() {
              const subcategories = this.querySelector('.subcategories');
              subcategories.classList.toggle('open');
              
              // Close other open subcategories
              categoryCards.forEach((otherCard) => {
                  if (otherCard !== card) {
                      otherCard.querySelector('.subcategories').classList.remove('open');
                  }
              });
          });
      });

      // Search functionality
      const searchInput = document.getElementById('searchInput');
      const categoryGrid = document.getElementById('categoryGrid');

      const filterCards = () => {
          const filter = searchInput.value.toLowerCase();
          const cards = categoryGrid.getElementsByClassName('category-card');

          Array.from(cards).forEach(card => {
              const title = card.querySelector('.category-title').innerText.toLowerCase();
              const description = card.querySelector('.category-description').innerText.toLowerCase();
              if (title.includes(filter) || description.includes(filter)) {
                  card.style.display = '';
              } else {
                  card.style.display = 'none';
              }
          });
      };

      searchInput.addEventListener('input', filterCards);

      // Also filter when search button is clicked
      document.getElementById('searching').addEventListener('submit', function(e) {
          e.preventDefault();
          filterCards();
      });
  });
  </script>
</body>
</html>
